import * as genHttpTool from "./gen-http-tool"
import * as storageTool from "./storage-tool"
import * as urlTool from "./url-tool"

export default {
	url: urlTool,
	genHttp: genHttpTool,
	storage: storageTool,
}
