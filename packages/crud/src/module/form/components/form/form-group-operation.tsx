import React, { memo } from "react"

type Props = {
	/**
	 * 是否显示`删除`按钮
	 * @default false
	 */
	visibleDelete?: boolean
}

const FormGroupOperation: React.FC<Props> = memo(() => {
	return <></>
})

export default FormGroupOperation
